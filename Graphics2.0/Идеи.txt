Идеи:
    --1) Сделать так,чтобы размеры канвас были зависимы от размеров окна        = нет необходимости
    --2) Связать кнопки на клавиатуре с экранными
    --3) Сохранение состояния при включении и выключении приложения (по старт и стоп дергать файл)
    --4) Сделать больше/меньше
    --5) Сделать дробное число (смысл: если это число, значит пройтись по нему для поиска точки
         если точка найдена, то записать число в стринг, а затем попробовать привести к дабл;
         если точка не найдена, то сразу записать в инт)
    --6) Перед лейблом поставить элемент с выпадающим списком (чтоб можно было его выбрать и отобразить)
         он будет показывать по умолчанию f(x), а в выборе f(y). В зависимости от этого строится график
    --7) Сделать модуль
8) Сделать скобки
    --9) Сделать всплывающие подсказки по наведению мышки на элементы, подсказки где-то внизу всплывают
    --10) Попробовать сделать анимацию на текстовую подсказку в лейбле
11) Добавить минус
    --12) Прочитать учебник Зуй, понять бинарное дерево и можно ли его использовать у меня
13) Сделать вычисление сложных выражений (смысл: вычисление сначала выражения в скобках, потом умножение/деление,
    затем сложение/вычитание, затем заменять их на результат, после чего высчитывать конечное выражение с перменной)
    --14) Может проще разрешать писать в формулу все, что угодно, а в отдельной строке обрабатывать и изменять ее? или как то 50 на 50              = так не проще
    --15) ЕСЛИ 14 НЕ ДЕЛАТЬ: Может целесообразно обработчик нажатия каждой кнопки прописать в отдельном файле, где не графика (смысл: посылать      = на данном этапе нет
        туда нажатую кнопку и текущую строку, там обрабатывать собывтие, решать можно написать так или нет, возвращать тру или фолс,
        в зависимости от этого добавлять знак в формулу или нет)

Баги:
    --1) Отладить алгоритм
    --2) Стереть график по отчистке
    --3) При построении графика в измененном масштабе, то он его не учитывает, пока не покрутить колесиком
    --4) Перед построить необходимо стереть предыдущий график (перечертить линии сетки с масштабом)
    --5) Можно поставить несколько точек в одном числе (9.5.6)
    --6) Если после точки идет сразу знак, то добавить 0 после точки или удалить точку
    --7) Чтение из файла падает при записи в файл выражение с y
    --8) При построении функции числа, а потом при изменении функции и построении опять же функции числа, то график не меняется
    --9) Поправить размер тултипов - везде разный
    --10) Для "больше" знак не заменяется, а добавляется в конец
    --11) При наведении на нижнюю часть больше/меньше правая часть начинает подниматься вверх, из-за того, что в лейбле появляется двустрочный текст
    --12) Когда ничего не введено, строит функцию f(x)=0
    --13) После того как 2 раза поставил модуль и последний удалил, то больше не ставится
    --14) Если написан только один модуль в формуле, то в конец добавить еще один и переписать формулу
    --15) После х невозможно закрыть модуль
    --16) После открывающегося модуля можно написать знак
    --17) После любого знака можно поставить любой другой неограниченное число раз
    --18) Если после 0. написать х, то он пишет 0*х
    --19) После закрывающегося модуля нельзя написать знак
    --20) Если написать сначала цифру, потом больше/меньше переменной, то результат выдается неправильный 8<x
    --21) Если написать выражение с модулем, затем приближать его, то точка возле оси Х начинает прыгать
    --22) Когда мы очищаем формулу или удаляем все знаки модуля, то график строится все равно по модулю (isAbs сделать в false)
    --23) Если выражение такого типа |x|+5, то парсер доходит до + и проверяет символ перед ним, это оказывается не икс,
          значит х справа, и пытается число (слева) привести к double и сыпается, т.к. на этом месте находится х
    --24) Ставится число после переменной в формуле
    --25) |x|+5 не работает, есть стереть частично |x+5|
    --26) 5-|x| строит не правильно, т.к. нет разделения на стороны от знака
    --27) Не работает: число операция переменная (5+х), в обратном порядке работает
    --28) При введении несколько операций подряд, то вместо замены предыдущей на последнюю - просто ничиего не вводится
    --29) Можно ввести <>
    --30) Если написать иск с операцией, без цифры, то сторится график будто бы там стоял 0, надо дописать 0 в конец
    --31) После операции можно ввести <> (x-<)
    --32) Если написать х-|-5|, то первый | заменяется минусом и получается х--5, модуль написать невозможно
    --33) Деление на 0 и так не строит, это нормально, но необходимо вывести сообщение об ошибке
34) Убрать добавление элементов в конец выражении, если оно не валидно, удалить последний символ

Текущие:
    --1) Сделать так, чтобы кнопка стала доступной после ввода текста в текстовое поле
    --2) Сделать кноки масштаба (мб получится сделать изменение масштаба колесиком)
    --3) Разобрать механизм трай и кетч и создать класс, который будет проверять число или нет, затем возвращать тру или фолс, сделать его static
    --4) Разобраться почему так работает сравнения!
    --5) Как у лейбла выводить текст справа
    --6) Посмотреть, как делать структуру на java
    --7) Разделить граф часть и вычисления на разные классы
    --8) Повесить пиктограммы на кнопки
    --9) Вывод подсказки на кнопке, когда она заблокирована      = невозможно
    --10) Для "+" неважно с какой стороны х
    --11) Добавить +- на форму = не нужно
    --12) Доделать с делением на х, чтобы он не рисовал вертикальную линию возле 0, график получается не симметричный - ПОПРАВИТЬ
    --13) Блокировать х и у в зависимости что выбрано в чекбоксе
    --13) Реализовать кнопку y
    --14) Построить просто х и у
    --15) Построить просто число
    --16) Создать множество - TreeList - в котором будут храниться числа от 0 до 9 и еще одно множество
          в котором будут храниться знаки, потом путем isContain проверять принадлежность, возможно сделать ввиде класса с возвращением, static
    --16) Если число перед х, то тогда передать это выражение в функцию умножения (*, R, число)
    --17) Доделать чтение из файла
    --18) Запись в файл по кнопке "Построить"
    --19) Посмотреть можно ли вставить символы через их коды
    --20) Доделать больше мньше с цветом
    --21) Менять тип линии при меньше/меньше или равно
    --22) Сделать разницу между меньше/больше = закрасить дургую сторону
    --23) Сделать тоже самое для игрика, что и в 22
    --24) Поменять компановку, чтоб подсказки всплывали внизу
    --25) Попробовать сделать код для подсказок в одном блоке
    --26) Доделать подсказки так, чтобы менялись в зависимости от текущего текста кнопки
    --27) Баг 23 исправить
    --28) Может быть получится перенести часть функции x_plus в doAbs, то что связано с модулем, путем интегрирования строчек кода туда = так нельзя
          т.к. в doAbs работает не зависимо от знака в выражении
    --29) Перенести логику на другие операции
    --30) Перенести логику на игрик
    --31) Доработать, как построить график просто x и |х|
    --32) Для минуса сделать резделение на стороны от знака
33) Минус со всеми операциями, кроме + не работает
    --34) Минус перед х не работает
35) x+-5 и x--5 одно и то же
    --36) x+5 не правильно чертит
    --37) x+5 и х+-5 одно и то же
